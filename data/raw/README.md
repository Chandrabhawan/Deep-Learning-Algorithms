# Raw Data

This directory holds downloaded datasets (MNIST, CIFAR-10, IMDB, etc.).

Datasets are downloaded automatically by the notebooks when needed.
Large files are excluded from version control via `.gitignore`.
